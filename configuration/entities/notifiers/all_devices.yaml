---
# This file contains a list of devices that should be notified when ALL_DEVICES group is called.

- name: ALL_DEVICES
  platform: group
  services:
    - service: mobile_app_sam_s_phone
    - service: mobile_app_pixel_4a
